# Server-only. Do not prefix these with VITE_.
# On Vercel, add them as secrets (visibility: secret). They are not shipped to the browser.
# Use the Project URL and the anon/publishable key from the same Supabase project.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-or-publishable-key
